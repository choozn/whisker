# What is whisker?
Whisker is a set of bash util functions for linux to group `.config` files into bundles.
This means that config files can be tied together with their corresponding install scripts.
The bundles will stay in the location where they were installed and will be linked to the locations specified inside of the `bundle.conf` file.

# Credits

